spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workflowx
    username: postgres
    password: sayanbanerjee2003

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
